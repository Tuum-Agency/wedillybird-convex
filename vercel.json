{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -n \"$CONVEX_DEPLOY_KEY\" ] && [ \"$VERCEL_ENV\" = production ]; then npx convex deploy --cmd 'pnpm run build'; else pnpm run build; fi"
}
